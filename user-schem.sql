CREATE TABLE users(regid SERIAL PRIMARY KEY, firstname VARCHR(45) NOT NULL, lastname VARCHAR(45) NOT NULL, email VARCHAR(45) NOT NULL, mobile VARCHAR(10) NOT NULL, status VARCHAR NOT NULL DEFAULT 'active')